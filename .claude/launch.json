{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "lantern-dev",
      "comment": "Preview-tool dev server: runs the main checkout's server from the project root (relative cwd, required by the sandboxed preview tool). The isolated C:/dev/lantern-os-dev worktree is launched separately by scripts, not by the preview tool. Uses server-dev.js (not server.js) so the launcher-injected PORT env var is stripped before boot, otherwise session-secret.js refuses to start (PORT set => non-loopback => SESSION_SECRET required). See docs/DEV-SERVER-WORKTREE.md.",
      "runtimeExecutable": "node",
      "runtimeArgs": [
        "--watch",
        "apps/lantern-garage/server-dev.js"
      ],
      "port": 4178,
      "autoPort": true,
      "env": {
        "CHAT_TOOL_EXEC": "1"
      }
    },
    {
      "name": "lantern-preview",
      "runtimeExecutable": "node",
      "runtimeArgs": [
        "--watch",
        "apps/lantern-garage/server.js"
      ],
      "port": 4177,
      "autoPort": true,
      "env": {
        "LEADERBOARD_ROUTING": "1"
      }
    },
    {
      "name": "lantern-verify",
      "comment": "Σ₀ surprise-valve verify (#1687/#1702). Fixed port 4191 (NO autoPort) so preview REUSES the already-running, key-hydrated branch server launched from C:/dev/lantern-os-valve-verify (SURPRISE_CANARY=1, Vertex, CHAT_TOOL_EXEC=0). runtimeArgs point at that worktree's verify-server.js only as a fallback if the server isn't already up. Disposable — not for prod.",
      "runtimeExecutable": "node",
      "runtimeArgs": [
        "C:\\dev\\lantern-os-valve-verify\\apps\\lantern-garage\\verify-server.js"
      ],
      "port": 4191,
      "env": {
        "SESSION_SECRET": "valve-verify-throwaway-not-for-prod"
      }
    }
  ]
}
