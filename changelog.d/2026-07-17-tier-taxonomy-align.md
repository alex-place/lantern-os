### Changed

- tiers/docs: aligned the last stale references to the **retired $5 "Member"** tier with the currently sold ladder — **Free / $20 Pro / $200 Pilot** (audit findings 43/54/57/58). No behavior change: the `$5 → supporter` Patreon mapping and every threshold/entitlement value are untouched — `supporter` is retained for legacy patrons and already sits at the Free plan floor (`plan-matrix.js` `ROLE_TO_PLAN`); only labels, comments, and docs move. **(#2644)** `routes/pages.js` `TIER_LABEL` no longer names the dead "Member" tier on the upgrade wall — a (latent) `supporter`-level gate now reads "Pro", the cheapest plan a paying upgrade buys. **(#2655)** `lib/auth-providers.js` and `lib/role-hierarchy.js` comments now state the $5→supporter mapping is retained *legacy-only* (not a sold tier), so a future contributor neither "restores" the Member offering nor deletes the mapping and strips legacy patrons. **(#2658)** swept `docs/PATREON-OAUTH.md`, `docs/USER-PROFILES.md`, `docs/AGENT.md`, and `CLAUDE.md`: the pledge→role tables now show the accurate three-row mapping ($20→`deep_dreamer` Pro, $200→`pilot` Pilot — the old docs wrongly folded $200 into `deep_dreamer` and omitted `pilot`), with the $5→supporter row demoted to a clearly-marked legacy note. Verified: `patreon-tier-amount`, `tier-entitlements`, `stripe-billing` suites green; md-link gate clean.
