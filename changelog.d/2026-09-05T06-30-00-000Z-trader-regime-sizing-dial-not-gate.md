### Added

- trader: **regime sizing — the morning read as a dial, not a gate.** SPY's close position within its first 30 minutes (known by 10:00 ET, no lookahead) reads the day; at or below `TRADER_REGIME_R30_MAX` (default 0.15) every later LONG entry carries `TRADER_REGIME_SIZE_MULT`× risk for the rest of the session — entries are never blocked, inverses (with the tape) stay full size, and the engaged read journals one `regime_size` row per day so the arm is scoreable. Measured on the 60-session armed-parity replay with **identical trade lists** (pure size isolation): 0.25× @ 0.15 adds +0.20pp return at maxDD 1.11%→0.96% (−13%); thresholds ≥0.20 lose (mildly weak opens still pay longs — only the extreme bottom marks a trend day), and the effect is dose-monotone (0.75×→0.5×→0.25×). The gate form of this same read was measured dead long ago ("predicts 86% but throws away winners"); the dial form is the shape that passed, same as the VIX stress multiplier. Default OFF; the same PR files the ledger rows for this arm (due 09-19) and for the Thompson/posterior slot-allocator rejection (a rolling per-symbol mean is momentum at the allocation level — it lost on both its sampled and deterministic forms).
