### Changed

- trader-ui: the alert dialog is rebuilt to TradingView's standard — one control height throughout, custom select chevrons and checkboxes replacing the OS widgets, a monogram symbol pill, a single group divider instead of a hairline under every row, and no blue focus ring on open (#3355)
