### Removed

- repo-slim: removal wave 3 (#2540) — 30 zero-reference orphans across five audited groups: discord dead modules (`rp_bot.py`, `music_queue.py`, `mcp_bridge.py`, `mcp_connector.py`, `bot_tools.py` — superseded by `bot_v2.py` + MCP curators), one-off PDF/audio/image generators + SD/LoRA one-shots, superseded training/eval (`lightning_train_*`, `test_ouro_load.py`, `measure_drift_845.py`, `eval_fc_indomain.py` + `rebalance_fc_corpus.py`, …), the remaining root stray (`check_gpu.py` — six others were already gone), and misc src/apps (`work_dispatcher.py`, `slot_loader.py`, `three_doors_classifier.py`, `generate_door_lora.py`, `src/lantern-desktop/*.py` per ADR-0014, `highlight_detector.py`, `generate_blackbox_program_whitepaper.py`). **Excluded:** every July-2026-dated orphan on the audit's do-NOT-remove active list. Verified: zero references from live surfaces or tests (the only two referrers are themselves deleted by waves 1–2); pytest collects 1,551 tests with only the pre-existing local scipy gap. Founder sign-off: operator-directed 2026-07-17; recoverable via git history.
