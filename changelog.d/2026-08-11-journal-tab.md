### Added

- trader: **the Journal tab** (#3242, #3243 v1) — a sixth tab in the terminal's bottom panel and a `Journal` slot in the Watch/Trade/Options sub-nav (deep link `/stock-trader.html#journal`). Realized results at a glance: headline stats with max drawdown at equal prominence and the honest risk-exit win rate beside the headline one, the equity curve by exchange day, a five-week daily-P&L calendar, a breakdown table sliceable by exit reason / ET hour / symbol (plain-English labels, structural win rates flagged, confirmed-vs-all-decisions toggle), the skip log — every declined trade grouped by reason — and the always-visible exclusions line. Avg MFE/MAE tile is null-honest ("accruing") until #3241 data builds history.
- trader: **demo-mode journal for guests** — logged-out visitors get the *simulated* demo book's journal (deterministic `champion-demo.journalRows()`, served via the sanctioned `?demo=champion` pattern; the real ledger is never readable without entitlement, per the 2026-08-11 no-public-ledger decision) with a "your account gets the real one automatically" banner and a See-plans CTA. The `#journal` arrival suppresses the Pro-gate overlay for this read-only surface (the wider gating collision stays tracked as #3008).
