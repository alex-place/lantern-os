### Changed

- Day P&L tile stops lying after the broker's evening reset: fallback figures are labeled as broker-basis, realized never shows the paper API's constant zero as a measurement, and the positions route honours TRADER_TRADES_LOG so any server can compute today from the real ledger
