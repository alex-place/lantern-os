### Fixed

- Payments/tiers now deliver what pricing sells (#2992 reconcile). (1) Own-broker Alpaca connect is sold as a Free-tier capability and hasEntitlement grants 'trade' once a broker is connected — but the server gated the connect endpoints behind the 'trade' entitlement a Free user could only earn BY connecting, a chicken-and-egg deadlock. The /api/broker/alpaca/* connect/status/disconnect endpoints now require only a signed-in account (requireAuth), matching /api/files/*; order placement stays trade-gated on /api/trading/*. (2) The legacy 'founder' role (a level-2 alias for the 0 deep_dreamer) was missing from chat-quota CAPS, so founder patrons were throttled to the Free 50/day instead of the Pro 250/day — added. The PLAN_ENFORCEMENT flag half of #2992 was already removed in #3134 (enforcement is unconditional).
