### Changed

- Chat's native tool loop now re-queries long-term memory against the evolving tool context between steps (per-step memory refresh), so later steps get memory relevant to what the turn became
