### Fixed

- trading: **a stop that fills while the brain is down now books at the fill.** Race carried SQQQ (396 @ 38.75, GTC stop 37.59 placed Thursday) into Monday 2026-09-21's gap-up; the stop filled 09:31–09:33 while the server was disarmed, the brain re-armed at 16:20 and never booked it, and the phantom sat in `lastPos` journaling "external-close sweep deferred" on every scan of the empty book, to be reconstructed at a stale mark whenever the account next held anything. Three guards, each sound alone, lost the trade together: `getEngineOrders` asked Alpaca for closed orders submitted in the last 30 hours, and `after` filters on submission time, so a Thursday stop was never in the feed; the fill ledger's process-start guard drops any fill older than the process; the sweep's trust guard defers on a 0-row snapshot. Now the feed reaches back a week (a GTC stop on a multi-day carry is submitted days before it fills), a pre-boot fill of a position the brain is tracking (known entry price, fill after its open time) is treated as the exit it was waiting for rather than history, and a fill-closed symbol is dropped from `lastPos` (and its absent streak) inside the fill reconciliation itself, so an empty book can neither hold the phantom nor reconstruct it a second time. `test/fill-visible-exits.test.js` pins the ledger exemption and its conservative edges, the week-wide feed, and the brain on an empty book with a pre-boot fill (one row, priced at the fill, no deferral, phantom cleared, never double-booked).
