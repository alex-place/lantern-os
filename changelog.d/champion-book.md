### Added
- **Champion allocation engine, live on the paper Alpaca account (ADR-0027/0028).** `lib/champion-book.js` + `GET /api/trading/champion` (plan) + `POST /api/trading/champion/rebalance` deploy the backtested Champion: a momentum-tilted **shrunk-tangency** weight across the 8-ETF universe (SPY/QQQ/IWM/EFA/TLT/GLD/XMMO/SPMO), scaled by the streaming vol-target brake's live gross (`brake-monitor` grossTarget, 0–2×), rebalanced with a no-churn band into fractional orders. **Governance:** PAPER ONLY (hard-refuses a live account), **DRY by default** — places nothing unless the caller arms it AND the operator sets `CHAMPION_ARM=1` ("a person always decides"); real capital stays gated on the ADR-0028 Sharpe-CI mandate. Every rebalance is logged to `champion-book.jsonl` so the gate can score the paper track record. `alpaca-adapter` gains `cancelOrder`/`cancelOpenOrders` so a rebalance sell frees shares reserved by another engine's resting orders (shared-account fix). Verified live: dry plan against the real paper book, and armed paper buy + sell legs both accepted. Tests: `test/champion-book.test.js` (10 — allocation math, no-churn band, gross clamp, and the arm/dry/live-refuse gates).
