### Changed

- docs: re-ground the entrypoint table against the running system — it described a Railway deployment that does not exist and a port-5050 child process that was deleted, while omitting the real GCE production origin
