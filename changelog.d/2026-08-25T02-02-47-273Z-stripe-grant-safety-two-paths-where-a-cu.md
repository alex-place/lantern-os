### Fixed

- Stripe grant-safety: two paths where a customer could be charged but granted nothing are closed. (1) With SOME tiers configured by Price id but one not, checkout still created an inline ad-hoc price for the unconfigured tier and charged for it — but the webhook's roleForPrice() refuses the USD-amount fallback once any Price id exists (anyConfigured, #2620), so that tier granted nothing. lineItemForRole now refuses symmetrically: a tier with no Price id is not purchasable once another tier is configured. (2) /api/billing/config now reports webhook:!!STRIPE_WEBHOOK_SECRET so pricing/an operator can see when the source-of-truth grant path is disarmed (secret key set but webhook secret unset → checkout charges, webhook 503s, only the fragile client sync grants).
