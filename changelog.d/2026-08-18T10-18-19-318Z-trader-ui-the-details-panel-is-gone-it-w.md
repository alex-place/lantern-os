### Changed

- trader-ui: the Details panel is gone. It was added because TradingView has one, without deciding what ours would show — and the watchlist's own info panel already carries the symbol, exchange, price, RVOL, technicals and returns it would have duplicated. A stored choice of details falls back to the watchlist rather than leaving the dock blank (#3355)
