### Added

- growth: refer-a-friend attribution + conversion (#2554, Level-3 mechanism). `lib/referrals.js`: a per-user **signed** referral code (keyed HMAC of the user id — unforgeable, storage-free, no PII in the link), attribution of a referred signup into the traction ledger as a MEASURED `referral_signup` event (referrer→referee, one referrer per referee, no self-referral), and a conversion tally where a referee **converts only when they cross the composite-active bar** (#2547) — the anti-gaming gate, so an account that never became real is never reward-eligible. **Reward issuance stays a founder call** (`rewardIssuance: "founder_decision"` — the module never auto-grants comp), matching the plan's Level-3 deferral. Wired: `?ref=` captured on the landing page (first referrer wins, persisted across the auth redirect) and forwarded by the register client; `GET /api/referral/link` (session user's share link) + a copy-button card on the profile page; operator-gated `GET /api/referral/report`; and the signups/conversions fold into the weekly `weekly_rollup` + `/api/traction/level1` snapshot. Verified: 6-case unit suite (unforgeable codes, attribution, self/double/bad-code rejection, anti-gaming no-convert-until-active, conversion flips on active, no-PII link) + full as-a-user e2e on a dev server — real accounts, referred signup via `?ref=`, attribution recorded, conversion flipped exactly when the referee became active, operator report + 403 for guests, profile card renders with a working copy button.
