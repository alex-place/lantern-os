### Fixed

- watch: **a chart slot no longer waits forever for a symbol the feed doesn't carry — and no longer crashes signed-in visitors.** Chart slots persist in the browser while the watchlist lives server-side, so the two drift (a server-data reset, or a slot set before the slot editor started registering symbols): the collector only polls listed symbols, so the orphaned slot sat on "Waiting for price data" forever (#3514). On load the page now re-adds any slot symbol the list is missing — the same call the slot editor makes — and the chart starts flowing within one collector poll; a symbol that still can't be added (invalid, guest, cap) now says "No live feed for X — add it to your watchlist" instead of implying data is on the way. Separately, `setOrderSymbol` still wrote to the order-ticket form that was cut when this page split from the trader as the watch-only surface — every signed-in visit threw `Cannot set properties of null` at boot (the one console error in the issue capture). It now no-ops without the panel, matching the page's own `openOrderTicket` redirect.
