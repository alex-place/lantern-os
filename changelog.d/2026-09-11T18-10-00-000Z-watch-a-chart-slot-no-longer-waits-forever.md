### Fixed

- watch/trader: **a chart slot no longer waits forever for a symbol the feed doesn't carry — and the watch page no longer crashes signed-in visitors.** Chart slots persist in the browser while the watchlist/tradelist lives server-side, so the two drift (a server-data reset, or a slot set before the slot editor started registering symbols): the collector only polls listed symbols, so the orphaned slot sat on "Waiting for price data" forever (#3514). Both chart surfaces now tell the two states apart — "Waiting for price data" only before the feed's first answer; after it, "No live feed for X" with a one-click **Re-add X** button. The repair is deliberately user-initiated rather than a silent boot mutation: each page routes the button through its own `addWatchlistSymbol`, so on the trader page — whose list is the guarded AI tradelist — the tradelist-edit warning still gates membership, and server-side lists never change without an explicit action. Separately, `watch.html`'s `setOrderSymbol` still wrote to the order-ticket form that was cut when the page split from the trader as the watch-only surface — every signed-in visit threw `Cannot set properties of null` at boot (the one console error in the issue capture). It now no-ops without the panel, matching the page's own `openOrderTicket` redirect.
