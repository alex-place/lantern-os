### Added
- **Σ₀ discrete-time collapse dichotomy ([#1988]).** The collapse certificate's theorems are all continuous-time (`e^{tA}`, `solve_continuous_lyapunov`, split by `Re λ`), but §6's own evidence — and any step-by-step generator — is discrete-time, where contraction is `ρ(A) < 1`, not `max Re λ < 0` (§6 measures Jacobian spectral radius `ρ≈1.064`). New `src/cio_sde/collapse.py::discrete_dichotomy_certificate` (+ `DiscreteDichotomyCertificate`) closes that proof↔evidence mismatch: it splits `A` at the circle `|z| = 1 − δ` via an **ordered real Schur** factorization (orthonormal basis, no eigenvector inverse — so the split stays A-invariant to machine precision even for **defective** `A`, where the eig-based continuous path abstains), certifies the active block's per-step contraction with the discrete Lyapunov/Stein metric `AᴹᵀPAᴹ − P = −I` (`P ⪰ I` ⇒ decay factor `√(1 − 1/λ_max(P))`, transient `√cond(P)`), lower-bounds the discrete Kreiss constant, and decides the fate from the slow block's spectral radius `ρ_N` — `ρ_N > 1` DIVERGE, `< 1` COLLAPSE, `≈ 1` MARGINAL, no third fate. For normal `A` it reduces to `ρ(A) < 1 ⟺ collapse`; discretizing a continuous flow as `e^{A·dt}` preserves the fate. Machine-checked by 3 suite tests (`tests/test_cio_sde.py::test_discrete_dichotomy_{radius_trichotomy,matches_continuous_under_exponential,defective_split_is_invariant}`; suite 42 → 45 passing, 0 xfail). Scope unchanged and honest: certifies the **local linear Jacobian**'s discrete iteration, not a global guarantee — grounding remains the safety mechanism; "machine-checked" = closed-form algebra + tests, not Lean. Docs: `docs/SIGMA0-COLLAPSE-CERTIFICATE.md` §6 gains the discrete-cert note + a dated maintenance log; three sibling frontiers filed ([#1989] defective-`A` retrofit of the continuous path, [#1990] trigger→theorem, [#1991] local→global ROA).
