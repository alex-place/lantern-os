### Fixed

- trader: the re-protect cap now bounds stop accumulation for ANY broker status, not just the failure words it knew. It recognised failure by allowlist (`inactive|reject|needs_confirm`), so a status in neither vocabulary — an empty/absent status field, or any term the broker uses that we had not seen — was invisible twice: not WORKING, so the pass placed another stop; not a known failure, so the cap never counted it. Live 2026-08-18: QQQ held 80 shares behind 1,200 shares of resting stops and SPY 75 behind 1,125 — 15 duplicates each against a cap of 3. Status is now classified by complement (WORKING, else TERMINAL lifecycle, else FAILED), so unknown lands on the safe side; `cancelRestingStops` also clears parked/unknown duplicates instead of only submitted ones
