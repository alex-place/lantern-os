### Fixed
- Leaderboard provenance stamp (`scripts/eval_ledger.py`, #2108) no longer reads the `OURO_*` serving env unconditionally: `served_checkpoint` is now resolved from the row itself — Ouro engines (`loop`, `ouro-*`) inherit the env, chat rows use a unanimous `served_models` histogram, and every other engine stamps the run's own `--model` arg. This ends contradictory rows like `qwen25coder-onbox-2173` (`model: qwen2.5-coder:latest` stamped `served_checkpoint: ouro@checkpoint-600` because the box was configured to serve Ouro). The ledger is append-only — historical rows keep their pre-fix stamp; trust a pre-fix row's `model`/`served_models` when they disagree.
