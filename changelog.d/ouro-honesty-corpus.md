### Added
- **Updated-Ouro honesty corpus + local golden-bench wiring (the ideal-model path).** `experiments/sigma0_ouro_honesty_corpus.py` builds the S1 honesty training set in exactly the `train-qlora-ouro.py` format (### Instruction/### Response): 343 rows = 93 golden facts + 250 verified project tuples, targets in the golden benchmark's own CLASS:/VERIFIED: format so honesty transfers to the scored task. **66 golden facts are held out** (`data/sigma0/ouro_honesty_heldout_ids.json`) — zero train/eval overlap, machine-checked (6 tests: determinism, disjointness, no class inflation, negatives >=20%, trainer schema). `sigma0_live_bench.py` gains an **Ollama caller** ('Ouro local (ours)') + `--heldout-only`, so the current and the honesty-tuned Ouro get golden rows on never-trained items. Candidate updated base: Ouro-2.6B(-Thinking) (ByteDance, Apache-2.0). Honest scope: the current-Ouro baseline row is UNMEASURED — local serving is broken (default env has CPU-only torch; `.venv-train` missing bitsandbytes), and training runs on cloud A10/L4, not this box. Strengthens **Reason + Verify**.

