### Changed

- settings/trading: the **active-broker switch** (Auto / ⚡ Alpaca / 🔗 IBKR — which connected broker routes your trades, via `/api/broker/preference`) now lives in **Settings → Connections**, in the Brokerage group next to connect/disconnect. After the profile→settings consolidation the switch was stranded on `orchestration.html#broker` while the trader's broker pill promised "switch brokers in Settings" — this closes that gap. The switch renders only when a broker is connected, highlights the current choice, shows which broker "executes your trades now", and disables a broker you haven't linked yet. The stock-trader dashboard broker pill (top bar / ☰) now links to `/settings.html#connections` (was `orchestration.html#broker`), matching its own tooltip; it remains a live status indicator + one-click shortcut. `apps/lantern-garage/public/{settings,stock-trader}.html`.
