### Changed

- greenpath: MAIL_DISABLE=1 mailer kill-switch (survives .env.local override) + s7 rewritten to exercise the real BYOK Alpaca connect journey (connect-keys -> connected -> disconnect); gate GREEN 18/18
