### Changed

- trader: candles at TradingView's density — each range opens with its own interval (1D→1m … All→1mo), the window is a span measured from the bars, a right margin of empty slots is on by default, regular hours by default with an RTH/ETH switch on the strip, and the range buttons are one-shot
