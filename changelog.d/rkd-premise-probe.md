### Added
- **RKD (#1594) premise probe — evidence-first defer.** Before building the index-dependent retrieval-keyed delta, `experiments/csf_compression_benchmark.py::probe_rkd_premise` tests its load-bearing premise on real `data/csf_memory` logs. Result (recorded in `docs/research/2026-06-29-csf-beating-zstd.md` §2.1): every memory log fits inside zstd-19's window (16KB→128MB window = ≤3.7% gain), and an over-credited global-clustering RKD oracle still loses to col-best on the realistic logs (raw.jsonl +2,688B). RKD has no headroom until a single log exceeds ~the zstd window (~8MB); **deferred**.
