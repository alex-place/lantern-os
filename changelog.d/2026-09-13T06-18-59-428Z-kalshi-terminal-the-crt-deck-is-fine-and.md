### Fixed

- kalshi terminal: the CRT deck is fine and was measured wrong -- the light page around it was not. Signal colours scoped, a skin that overrides a tint now owns the text on it, and two double-dims removed from shared chrome.
