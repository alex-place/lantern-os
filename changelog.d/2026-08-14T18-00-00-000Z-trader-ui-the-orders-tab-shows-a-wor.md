### Changed

- trader-ui: the Orders tab shows a working-order count badge, matching Positions — "3 exits resting" reads as `Orders ③` without opening the tab (operator ask during the pre-open trim, where a duplicate resting sell and a silently-failed cancel both hid behind an uncounted tab)
