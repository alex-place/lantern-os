### Changed

- journal: real layout editing -- twelve columns instead of half-or-full, drag an edge to resize, drag a card to a position (including the end, which the old drag could never reach), and rows that fill and align (#3565)
