### Fixed

- chat: **the assistant claimed "Alerts are set" while creating nothing — because OpenAI was rejecting its whole toolbox.** On a loopback box every request is operator, so the MCP catalog was appended to the chat's native tools: 20 + 123 = 143 functions, past OpenAI's hard 128-tool request cap. OpenAI answered `400 Invalid 'tools': array too long`, a **silent** catch dropped the turn to the single-shot path with **no tools at all**, and the model — still holding live trader context and asked to act — invented tool outcomes ("Creating alert now… Alert created"), six turns in a row in the incident transcript, each one flagged by the ungrounded-confidence canary. Two changes: the MCP augmentation now fills only the room left under the 128-cap (native product tools — alerts, quotes, positions, docs — always make the cut), and that catch now logs *why* tools vanished instead of degrading silently. Verified end-to-end: "create a price alert for SPY above 775" now emits a real `trader_alert_create` call and the rule lands in `/api/trading/alerts/rules`.
