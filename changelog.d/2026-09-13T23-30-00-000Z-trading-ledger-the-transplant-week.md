### Changed

- trading: **the prediction ledger catches up on the transplant week** (26 → 31 rows). `morning-darkness` scored CONFIRMED on its due date — the 09-04 gate-relax sweep independently re-measured the morning gate under the full armed stack and the darkness protects the recent regime exactly as the 08-27 replay predicted (gate off → h2 +40→+22%). Five transplant/exit verdicts filed from the one-trader convergence work: `zone-ladder-under-stack` REVERSED (fired, and rejected harder — exit systems are ecologies, not organs), `deep-washout-bypass` REVERSED (cadence is an implicit persistence filter), `regime-dial-on-race` REVERSED (mechanism ports clean, payoff sign flips — risk policies are chassis-priced in value), `trail-arm-sweep` REVERSED (lowering the arm is strictly dominated; the paired small-gain variant costs 2.2pp at unchanged DD — the 9th early-exit rejection), and `shape-stable-remeasure` OPEN (the one transplant that passed: 5.80→6.45% with stable's V-day hole halved; arm decision held for the 09-19 checkpoint so the regime-size live row scores on a clean config).
