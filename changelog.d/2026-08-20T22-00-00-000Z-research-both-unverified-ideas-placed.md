### Research — both UNVERIFIED ideas placed by hand; zero of sixteen were novel

The audit could not place two of sixteen milled ideas. A hand review with live search placed both
in about two queries each: the verification head is UHeads (ICLR 2026, frozen base, LLM-generated
labels), and the cluster-routing idea is ELREA / GradientSpace / MoIN with the clustering source
swapped for a frontier API — a data-prep choice, not a mechanism.

So across the whole batch, nothing was novel. Two for two on UNVERIFIED here, on top of two for
two in the original red team: every idea the machine could not place turned out to be encumbered.
The mill's value is coverage with prior art attached, not invention.

The packet is recorded where priorwork.js indexes it, so the next audit inherits the finding
instead of rediscovering it — it is now the top prior-work hit for both ideas.
