Σ₀ LLM design of record (docs/research/2026-07-23-sigma0-llm-design.md): one pure looped
recursive core ≤3B/CPU-viable, verifier amplification + fixed cascade, the Collapse Certificate's
runtime gates as an enforced contract (JSRR on the default serve path, Σ₀⁻¹ armed, held-out
verification), MoE deferred behind a named switched-system admission gate (not rejected), ternary
ADR-0026 as the capability path, six registered falsifiers. Plus ADR-0031 (Spiral Coder →
ARC-AGI-2 budgeted-efficiency niche), the spiral ARC smoke test that reproduced the
memorize-passes-train/fails-held-out trap on a real exec run, and the Convergence World Model
experiments (honest negative: in this repo "merged" carries almost no diff-derived convergence
signal; the P(held) v1 with churn-back target + the repo's own Brier/ECE grader).
