### Changed

- trading: **the prediction ledger catches up to the week that armed three knobs and rejected two rules.** Four rows filed: `levcap-cap2` (REVERSED — the leveraged-concurrency cap fails the armed-parity replay: cap 2 flips h1 negative and worsens avg loss because capped slots refill with worse 1x trades; third cap-family rejection after #3354/#3396), `hourblock-off` (OPEN, due 09-15 — the 13:30–14:30 entry ban predates T2 and only removed T2-vetted flow; armed on stable 09-04 premarket, day 1 delivered +$821 from the freed hour), `cap6` (OPEN, due 09-12 — concurrent slots 5→6, armed 09-04 evening after Friday hit 5/5), and `tape-exit` (MISLEADING — the strongest-evidenced early-exit candidate yet, a tape-state feature separating stop-outs 71%/0%/9% on the ledger cohorts, still collapses in the engine at every threshold: return 5.60%→1.42%, maxDD doubled; the 7th early-exit rejection, and the cohort framing is named as survivorship). The due `t2-engine` row is scored CONFIRMED with its confound stated (decarry/invgate/hour-block armed in the same window; winner median MAE −0.06%→−0.01%, WR 62%→73%). `score_predictions.js` gains an opt-in `entered_since` metric flag so entry-side arms are scored only on trades entered after the arm — exits of pre-arm positions carry the old config's decision and flipped the t2 median (−0.05% clean vs −0.23% contaminated); the 17 already-scored rows keep the frame they were scored under.
