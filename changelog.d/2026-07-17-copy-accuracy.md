### Fixed

- copy accuracy (polish audit 1.11). **(#2484)** `pricing.html` Public-tier bullet read "…whitepaper, ADS" — a typo for **ADRs** (Architecture Decision Records, the term used everywhere else in the repo). **(#2490)** The trade-gate 403s returned `"…; Patreon login is disabled."`, which is both self-contradictory (Patreon is a live sign-in provider) and misleading (the real gate is a tier/entitlement, not a disabled login). When the site-wide Patreon auth gate is off, `lib/auth-middleware.js` now returns accurate copy: the entitlement gate says `"Account required — sign in, then upgrade your plan if this feature needs a higher tier."` and the role gate says `"Higher tier required. Sign in, then upgrade your plan to reach the <role> tier."` — the JSON shape (`error`/`entitlement`/`required`/`current`) is unchanged. Verified live (patreon flag off): both 403s return the new copy, and `pricing.html` serves "ADRs".
