### Changed

- docs: SCRIPTS.md regenerated from the Python scripts audit (#2537). The old inventory (2026-06-20) referenced `archive/` and `lantern-discord/` directories that no longer exist. The new registry is built from audit Appendix D (every wired `scripts/` file with its exact execution anchor, grouped by area: hooks/validators, eval & benchmarks, training/model lane, CSF/knowledge, media, trading/MCP) plus the §3 registrations of docs-only operational harnesses (SWE-bench/HumanEval chat harnesses, teacher-distillation + crystallization data prep, `eval_dashboard.py`, `models/three-doors-imagegen/generate.py`, `serve_minicheck.py`) — each row carries "how to run" + its anchor. The retired `validate-version-changelog.py` row is gone (removed with #2527); the likely-dead `scripts/orchestration/lantern-*.py` set is flagged as #2538 removal-wave candidates rather than registered. SCRIPTS.md is now itself a live anchor for the #2542 wiring tripwire — verified: a probe `.py` referenced only by a SCRIPTS.md row passes the gate (`✓ … ← SCRIPTS.md`). All 113 paths in the registry existence-checked; the md-link gate passes.
