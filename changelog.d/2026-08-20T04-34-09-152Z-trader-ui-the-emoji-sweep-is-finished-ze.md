### Changed

- trader-ui: the emoji sweep is finished — zero colour emoji anywhere the user can see, verified by walking every visible text node and every title / aria-label / placeholder at runtime. Toast messages dropped their decorative glyph (the words already said what happened), status dots became geometric circles that inherit colour, and the remaining pictographs became inline SVG (#3355)
