### Added
- **Walk-forward backtest + stop-width experiment for the stock autopilot (Verify).** `experiments/trader_walkforward.js` drives the REAL signal engine (`deriveDirection`/`rileyGate`/`convergenceVerdict`) and REAL exit logic bar-by-bar over historical 15m bars with **no look-ahead** (bars[0..t] only, entries fill t+1 open), longs-only, 0.10% round-trip cost, `stop = max(ATR×mult, 0.8%)`. Sweeps the ATR stop multiple (×2/×3/×4) with the knife filter on/off and writes `data/trader-walkforward.json` (equity curve + win/avg-R/PF/risk-exit-win/max-DD/Sharpe). First run (Jun 18–Jul 17 2026, 20 trading days, 16 names): **every config lands −92% to −95%, PF ≈0.7, Sharpe ≈−16** — wider stops help monotonically but trivially (~0.5pt/ATR step; ATR×4 best at −92.4%), the knife filter helps more than stop width (dropping it is worst, −94.9%). Conclusion: the negative edge is **robust to the obvious exit fixes** — the mean-reversion *entry* is the leak, not the stop. Honest caveats in the header (one 30-day window/regime, news/earnings/sector anchors neutral, 15m single-timeframe).
