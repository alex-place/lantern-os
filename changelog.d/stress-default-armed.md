### Changed
- trading: stress sizing is ON by default — x1.5 when the prior VIX close >= 25 (was: off unless `TRADER_STRESS_MULT` was set; VIX threshold default 20). Measured on all four lab surfaces (`experiments/armed_baseline.js`, VIX leg only): return AND return/DD improve on every surface; the 26-year holdout goes 136% -> 186% of return at a SHALLOWER max drawdown. Stress alone may now lift the per-position notional ceiling (cap x mult, e.g. 12% -> 18%) on stress days — `trading-guard.js` mirrors the same ceiling so sizer and guard keep agreeing; tier/tilt/regime remain down-only under the 2026-08-25 hard cap. The unmeasured SPY-IBS OR-leg defaults off (`TRADER_STRESS_SPY_IBS=0`). `TRADER_STRESS_MULT=1` disables stress sizing entirely. (Nagel 2012; #3428, #3433, #3445)
