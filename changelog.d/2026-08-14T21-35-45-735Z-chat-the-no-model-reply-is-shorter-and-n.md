### Fixed

- chat: **the no-model reply is shorter, and no longer guesses why.** When nothing can serve a turn, the assistant used to answer with 305 characters that named an internal port, explained what it is not ("a technical assistant, not a dream narrator"), and asserted a cause: that the cloud providers were *unreachable*. During the 2026-08-14 production outage that cause was simply wrong — the provider was reachable and correctly configured, and refused the call (`BILLING_DISABLED`). The reply is now 160 characters and says what is actually known: nothing is serving locally and no provider accepted the request, followed by the two things the reader can do about it. "No provider accepted the request" covers unreachable, unconfigured, and refused without claiming to know which.
