### Added

- traction: the Sean Ellis PMF fit-check (#2551) — the Level-2 gate instrument ("if the fit check fails, improve the product — don't spend on ads"). `lib/pmf-survey.js` + three endpoints: `GET /api/pmf/survey` (session user; records `pmf_prompted` and returns the survey exactly once — prompt-once is auditable from the ledger, no client-side heuristics), `POST /api/pmf/response` (one `pmf_response` per user ever; very/somewhat/not disappointed + optional free-text), and operator-gated `GET /api/pmf/tally` (n, counts, % very-disappointed vs the **40% bar**, `pass:false` when unmeasured — an empty log never passes a gate). Eligibility = the #2547 composite active definition (operator/test ids excluded). The weekly `weekly_rollup` and `/api/traction/level1` snapshot now carry the PMF section, so the fit check reports in the weekly traction review. Chat shows a dismissible bottom-right card (4s delay, confirm-click to send, thank-you) only when the server says `show:true`. Verified: 6-case unit suite + full as-a-user e2e on a dev server — registered real local accounts, seeded composite activity, card appeared, answer landed as a MEASURED event, reload did **not** re-prompt, duplicate answers rejected, tally math + operator gate correct.
