### feat(trading): shape-aware turn confirmation — V-bottom fast-track + weak-turn veto (default OFF)

Completes the operator's original T2 thesis ("different candle size, repeating
pattern, not low enough" — v1 implemented only the last clause). Behind
`TRADER_CONFIRM_SHAPE=1`: a capitulation bar followed by one strong reclaim bar
confirms immediately (a true V announces itself), and two rising closes that
reclaimed under a quarter of the drop are refused as drift. Fast-track
admissions journal `confirm_path` rows. Ships OFF: armed on the race box
(replay: WR 63→65, regime balance 79/21→39/61 at flat DD), NOT armed on
stable (same rules measured a negative first half there — mirror-image regime
signatures on the two gate stacks; the flag arms only per the receiving
brain's own replay).
