### Changed

- journal: the handle between two cards is now a divider -- it takes width from one and gives it to the other, so a 60/40 split is one drag and neither card is pushed onto the next row (#3569)
