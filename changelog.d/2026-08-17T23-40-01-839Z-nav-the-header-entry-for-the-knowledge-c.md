### Changed

- nav: the header entry for the knowledge center is now labelled Guide rather than Docs, matching what the page became — a beginner walkthrough of what unisona is and how to use it, not a documentation index. Docs sent people looking for API reference. The href is unchanged so every existing link still resolves (#3332)
