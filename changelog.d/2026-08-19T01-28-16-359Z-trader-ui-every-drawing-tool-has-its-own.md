### Added

- trader-ui: every drawing tool has its own settings now, not just fib and positions. All 35 drawable tools carry a spec — channels get a fill and a midline, text gets a font and background, a measure chooses which units to report, a pitchfork colours its median separately from its tines. Tabs with nothing to configure are disabled rather than silently showing another tab's fields (#3354)
