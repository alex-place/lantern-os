### Added

- spiral(#2867, #2868): **contractive-escalation enforcement + whole-answer confidence surfaced in chat.** Every VERIFIED escalation must strictly advance the ratchet or the run halts honestly with `haltReason:"escalation-noncontractive"` — the ILC ρ<1 condition (US7345448B2/US8094405B1): no runaway frontier spend on a plateau (dup-skipped escalations carry no new information and stay with the loop detector; knob `escalationContract`, default on). `spiral_solve` replies now carry the **whole-answer confidence** line — the best committed candidate's test pass fraction, decoupled from where the cascade stopped (HP US20120243734A1) — completing the #2862 harness field's path to the user. 38/38 spiral tests green (2 new contract tests; the sticky-mechanics tests pin their behavior with the contract explicitly off). Closes the shipped-elsewhere pair too: #2865 (stop-on-stall + state-hash loop detection) and #2866 (pass-terminates + de-escalation) landed in PR #2862 — verified against master and closed with evidence.
