### Changed

- trader-ui: each chart card's BUY/SELL moved off the footer row and onto the chart itself, top-left, the way TradingView overlays its order pills. That returns 36px per card to the chart — a 6.2% taller plot on every card in the deck. The pills hide while a drawing tool is armed so an accidental click cannot become an order (#3357)
