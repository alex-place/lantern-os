### Removed

- dead scaffolding (#2656, #2657) — 1,951 lines across 8 files, all confirmed unwired. **`public/app.js`** (orphaned; no page loaded it, its target DOM ids exist nowhere) and **`render-server.js`** (the "Render Lantern cloud mirror" retired in `manifests/retired-surfaces.md` — its `render.yaml` deploy companion was already deleted, leaving this 561-line file dangling). Both carried the stale `fallbackAccessModel` that priced a `supporter`-id tier at $20, colliding with the retired $5 tier and the live $20 Pro (`deep_dreamer`). **`payment-bridge/`** (6 files) — a standalone parallel Stripe-invoice service that nothing boots and whose `index.js` doesn't even parse (a pre-existing syntax error at line 324, so it has never run); it sold the retired `supporter` tier without the live Pro tier and, if booted with real keys, would bypass the canonical `routes/billing.js` tier-role guards + webhook whitelisting. Verified no references remain in server/deploy/CI/tests/surface-registry; its `config.json` held only placeholder keys.
