### Changed

- TRADER_IBS_EXIT (default OFF): in IBS mode a non-bullish read may signal-exit only once the session IBS reaches the validated bounce level (0.6) — closes the lab-to-live gap where NEUTRAL reads sold washouts ~50 minutes in for a median +0.09%
