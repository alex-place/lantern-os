- research(csf): best-in-slot improvisation prototype — push CSF-Omni's best-fit envelope down to per-column scheme selection (the BtrBlocks/CLP-S frontier recipe), [`experiments/csf_slot_prototype.py`](experiments/csf_slot_prototype.py), all variants lossless round-trip-verified. Measured on the realistic `raw.jsonl` (shipping CSF-Col→brotli = 22,002 B): v1 per-column **separate** streams **−5.4%** (framing + lost cross-column context), v2 per-column **transform + one brotli** **−3.3%** (a standalone-smaller transform like un-hexing the checksum *enlarges* the shared stream by flushing brotli's context — standalone-smallest ≠ joint-smallest), v3 **isolate the incompressible SHA-256 sector into a raw sidecar** **+2.5%** (the only real win; +18% vs zstd-19). Ceiling is ~2.5% not 2× because the archive is checksum-saturated + content already at 0.3 bpb — the same wall as #1596. Conclusion folded into `docs/research/2026-06-29-csf-vs-category-convergence.md`: best-in-slot is the right architecture but pays only on diverse-but-compressible columns at scale, and the grounded selector must measure *joint* size, not a per-column proxy.
