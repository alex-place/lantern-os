### Changed

- Feed-flap guard: the engine refuses a snapshot that looks like another account's book, books external closes only on two consecutive absences, and the bridge pins reads and orders to TRADER_IBKR_ACCOUNT
