### Fixed

- journal: **the empty state finishes its sentence.** "If you have already been trading, bring that history in:" was followed by nothing — the import offer (or the "connect a broker" line) was painted before the empty state rendered, and the render, like every Refresh, wiped it. The offer is painted after the render now, so the reader sees either the import button or where to connect a broker.
