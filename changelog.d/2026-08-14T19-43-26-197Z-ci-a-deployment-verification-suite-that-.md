### Changed

- ci: a deployment-verification suite that points at a RUNNING host — the first check here that can fail on configuration rather than code, plus a key-feature release gate and hourly production verification
