Σ₀ LLM: plain-English whitepaper (docs/SIGMA0-LLM-WHITEPAPER.md) for non-technical readers —
the design retold honestly with promises, confidences, and the can't-do list — plus the
operator's anytime-budget scope enhancement in the design doc (§3.2: per-task budget dial,
structured search over raw sampling, escalate-early on uncertainty, teacher-as-repair with
failing tests) and its own falsifier (the two-budget ARC experiment).
