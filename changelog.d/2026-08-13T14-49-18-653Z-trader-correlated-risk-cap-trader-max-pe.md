### Changed

- trader: correlated-risk cap (TRADER_MAX_PER_BUCKET) — counts concurrent positions by risk bucket (equity_long/equity_short/metals) instead of by symbol; ships DISABLED because the replay shows it trades income for tail 1:1 with no free lunch
