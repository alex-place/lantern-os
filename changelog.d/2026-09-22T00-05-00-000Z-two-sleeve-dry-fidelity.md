### Fixed

- trading: **the two-sleeve runner's dry mode never ran the brains, and its one scan ran under the wrong sleeve's thresholds.** Monday 2026-09-21's first dry session on race's account ticked 400 times without an error and produced not one row from either brain: `--dry` exported `TRADER_AUTO_EXECUTE=0` / `TRADER_MANAGE_EXITS=0` to the whole process, and both brains return "nothing to do" on those switches before they read the account or journal a skip, so the dry facade that was meant to catch every order was unreachable and "no dry orders" meant "no brain ran". The single shared scan ran this tree's signal engine under the process env (race's `.env.local`, IBS 0.15, default P_MIN) and starved the stable sleeve of every stable-threshold signal (0 signals at 14:12 while stable's live brain bought TLT at IBS 0.29) while feeding the race sleeve master's `scan.js` instead of race's. Now: the brains are armed in-process in every mode and dry-ness lives in the facade (`lib/two-sleeve/runner-support.js`: five reads forwarded, two writes refused and journaled as `dry_order` / `dry_cancel`, nothing else exposed, the LLM session review off in dry); each sleeve's scan runs in its own forked process (`lib/two-sleeve/scan-worker.js`) that loads that sleeve's app tree under that sleeve's env file and universe, so module-load constants like `convergence-ev`'s `P_MIN` and call-time knobs alike are the box's own, with the thresholds journaled on boot (`scan_worker_ready`); every `tick` row carries per-sleeve `signals` / `enters` / `executed` / `skipped` and a brain's early-return `reason`, so a hollow tick can never look like a quiet market; `TWO_SLEEVE_FAKE_NOW` pins a dry runner's clock for after-hours smokes. `test/two-sleeve-runner-support.test.js` loads the real brain to pin the gate and forks real workers to pin the isolation.
