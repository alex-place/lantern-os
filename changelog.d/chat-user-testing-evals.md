### Added
- **Chat user-testing eval layer — the human-preference, statistics, and long-session legs of Anthropic-style chat benchmarking (#1965, #1966, #1967).** (1) **Observe (#1965):** every finalized dream-chat assistant reply now carries 👍/👎; verdicts POST to the new `/api/dream/feedback` (`routes/feedback.js`) and append to `data/feedback/chat-feedback.jsonl` via the serialized file-queue (rotation-capped), each row attributed to the provider/model that actually served the turn (from the stream's `done` receipt) — so per-provider win rates become measurable instead of argued from vibes. Read-back at `/api/dream/feedback/recent` follows the #770 privacy precedent (per-session self-service; global read operator-only). (2) **Converge (#1966):** `scripts/eval_paired_diff.py` compares two per-problem eval detail files the harnesses already write by pairing on `task_id` — paired mean difference, SEM, 95% CI, win/loss/tie, exact sign test (arXiv:2411.00640 recs 1+4) — so leaderboard comparisons stop being bare means. (3) **Verify (#1967):** `scripts/eval_chat_drift.py` drives `POST /api/dream/chat/stream` exactly like the browser for a multi-turn session, sets a machine-checkable standing constraint in turn 1, and measures adherence per turn (`data/eval/chat-drift/` detail + one `chat-constraint-drift` leaderboard row) — instruction retention is now a number, distinct from the #1428 model-drift canary. Tests: `apps/lantern-garage/test/feedback-route.test.js`, `tests/test_eval_paired_diff.py`, `tests/test_eval_chat_drift.py`; both scripts ship `--selftest` offline proofs.
