### Fixed

- client polish (audit 1.11). **(#2483)** The home footer version badge (titled "View changelog") opened raw `/repo/CHANGELOG.MD`; it now opens the styled `/changelog.html`. **(#2495)** Removed leftover debug `console.log` from shipping pages so the console is quiet on normal use: 8 emoji debug logs in `create.html`'s inline script, the `[Kingdome]` log in `js/dream-chat.js` (and its `TELEMETRY.log` shim is now a no-op — `warn`/`error` still surface real problems), the XP-door log in `js/three-doors-game.js`, and the hot-swap log in `js/feature-runtime.js`. Deleted the fully-orphaned `js/accessibility.js` (74 lines, 8 `console.log` + 2 `console.error`, no referrers). Extended the pre-commit debug-statement scan **and** the CI `debug-pollution` gate to cover inline `<script>` in `.html` (the previous scan only saw `.js`, which is how create.html's 8 slipped in) — verified: the grep catches `console.log(`/`debugger;`, ignores prose, and passes the now-clean pages. Live: the home badge opens `/changelog.html` and dream-chat loads with a silent console.
