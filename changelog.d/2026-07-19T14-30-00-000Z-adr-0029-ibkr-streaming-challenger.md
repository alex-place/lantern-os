### Added

- ADR-0029 (Proposed): the IBKR streaming Challenger - a design to beat the Champion in returns, framed as a pre-registered duel rather than a promise. Three upgrades only, each receipt-backed: send-on-delta streaming cadence via the IBKR CPAPI gateway (the hourly-vs-daily brake already measured $51,355 vs $47,077 over 2.5y with 71h-faster de-lever), real IBKR tiered margin + bill-ETF cash sweep replacing the monitor's flat 3%/+150bp model, and minute-accurate split/dividend-correct history (Polygon, train 2004-15 / validate 2016-26) with survivorship-free cross-checks. Duel protocol: backtest win requires higher final value AND <= drawdown AND bootstrap CI excluding 0 AND 2-20bp cost robustness; then >=3 months IBKR-paper vs Alpaca-paper with the scoreboard in the monthly Leap; real dollars stay behind the unchanged ADR-0028 gate. Honest fold path: on a loss the friction-accuracy fixes are adopted into the Champion and subscriptions cancel. Retired paths stay retired (single-stock 12-1 momentum measured non-upgrade; gross >2x rejected) Phase A free-data variant RUN (experiments/challenger_free_rates.py + challenger_free.json): real ^IRX T-bill series (free, 1998-2026) replacing the flat 3%/+150bp carry, engine-equivalence $0.0000 - challenger $102,294 vs champion $91,702 (+11.6%) at shallower maxDD (-25.2% vs -25.5%), Sharpe 0.667 vs 0.648; bootstrap delta +0.34%/yr CI [-0.11,+0.74] P>0 93.2% (not significant at 95% - recorded honestly; deterministic accuracy fix -> fold-path adopts it); IBKR-Lite +250bp spread erases the win (broker tier is a return decision); ZIRP-era attribution documented in the ADR Cadence duel RUN (experiments/challenger_realtime_brake.py + challenger_realtime.json, free Yahoo hourly OHLC 2024-03..2026-07, wick-guarded with 131 close repairs + 3,118 low clamps): realtime intra-bar braking $51,543 vs hourly $50,902 (+$641, ~+0.5%/yr) at equal maxDD -19.8%, the entire edge from ONE mid-hour trend-gate cut (lambda 0.80 ~ 12min early, zero whipsaw); marginal-cadence curve measured end-to-end: daily->hourly +$4,239, hourly->realtime +$641 (diminishing returns as pre-registered); bootstrap +0.07%/yr CI [+0.00,+0.21] P>0 63% (fragile n=1 sample, recorded); bull-window caveat printed (static 2x won the window - brakes trail strong bulls); IRX carry costs this high-rate window (mirror of the 26y +11.6% gain)
