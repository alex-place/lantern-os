Σ₀-grounding verdict gains its ACTIVE FACE (Certificate Part IV / ACT-TO-KNOW): a stable-but-
unverified answer no longer rests at grounded=None ("I can't verify") — it PENDS with
next_action="experiment" (a directive to act: run the test / exec-verify / retrieve / escalate),
and only once every available means is exhausted (or the task admits no experiment) does it settle
to grounded=False — treated as EFFECTIVELY FALSE until an experiment proves it true
(falsificationist; not a claim of real-world falsity). The load-bearing invariant is unchanged
(grounded=True IFF loop-stable AND externally verified); the change only moves undetermined cases
toward the safe not-grounded side. New next_action ∈ {accept, experiment, escalate, halt} field
directs the loop. Refusal is not a resting place: verify it, or hold it false.
