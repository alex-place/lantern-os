### Added

- monetization: the four-plan capability matrix as a single source of truth + a dormant server-side enforcement layer (#2550, Level-2 gate mechanism). `lib/plan-matrix.js` encodes the product-plan §01 tier matrix as DATA — Free/Pro/Pilot(=Business) → capabilities, and the existing Patreon roles → plans (supporter $5 grandfathered to Free, deep_dreamer $20 → Pro, pilot $200 → Pilot) — with resolvers (`roleHasCapability`, `capabilitiesForPlan`, `planReport`). A new `GET /api/plan/report` exposes the matrix + the live-enforcement flag state so the founder can verify pricing.html matches enforcement 1:1. **Enforcement is OFF by default:** `hasEntitlement` consults the matrix only when `PLAN_ENFORCEMENT=1` (the founder's Level-2 activation, explicitly sequenced after Level 1 + BYOK #2505 per the plan); with the flag unset, behavior is byte-identical (existing `tier-entitlements.test.js` still green). Verified: 9-case per-tier unit suite (Free denied Pro/Pilot caps, Pro denied Pilot caps, monotonic ladder, legacy trade/ai_trader reconciliation) + live dev-preview in both flag states (report endpoint, guest 302 / authed 200 on the trade gate, no crashes).
