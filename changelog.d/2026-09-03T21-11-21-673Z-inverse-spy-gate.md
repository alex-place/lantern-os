### feat(trading): inverse-spy gate — no new inverse entry against a strong tape (default OFF)

The 09-03 diagnosis, quantified: every winning washout fired while SPY itself
was washed out (session IBS 0.08–0.15 at the moment); every big loss the
engine took was deep while SPY sat at its highs (SOXS entered at spyIBS 0.99,
−$3.3k). Depth anti-selects on trend days. `TRADER_INVERSE_SPY_GATE=<0..1>`
refuses a NEW SQQQ/SOXS/SPXS/TZA entry when SPY's session IBS at decision time
is at or above the gate; longs, exits, and held positions untouched. Ships OFF.
Measured per brain: stable +3.99%→+4.94% at gate 0.7 (payoff 0.77→0.88, both
halves positive, 6,070 refusals of the loss class); the race brain REJECTED it
(its fast exits monetize the same trade — both halves worse) — armed
stable-only per the propagation doctrine.
