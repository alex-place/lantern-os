### Fixed

- cleanup(#2525): `models/lantern-sigma0-coder/` is named after the **retired** Qwen2.5-Coder-3B QLoRA (weights deleted, #811/#823) but actually holds two *live* Ouro-coder training datasets (`humaneval-train.jsonl` ~15k rows, `coding-seed.jsonl`), so auditing `models/` by name misreads it as a stale model package. Added a `README.md` documenting the directory's true current contents and their writers (`build_humaneval_corpus.py`, `build_ouro_coding_dataset.py`), rather than moving the datasets and disturbing every reader/writer + the training scripts. Also fixed the stale docstring in `scripts/build_humaneval_corpus.py` that still cited the deleted `training-data.jsonl` (243 rows, removed in #1850). Relocating the datasets to a neutral home (`data/training/ouro-coder/`) remains an optional follow-up, noted in the README.
