### Fixed

- audit: repaired the remaining broken script references from the Python scripts audit §5 "fix now" bucket (#2543). (1) `lib/training-dispatcher.js`'s Colab training cell invoked `scripts/train_ouro.py`, which does not exist — the SAME file's Kaggle cell already uses the real trainer `scripts/train-qlora-ouro.py`; aligned the Colab cell to it with the trainer's actual CLI (`--resume`/`--max-steps`/`--seq`/`--out`, not the phantom `--resume_from`/`--seq_len`/`--output_dir`), so a dispatched Colab run no longer fails on a missing script. (2) `lib/convergence-lora.js`'s `triggerLocalTraining` references `scripts/train-convergence-lora.py`, which has never existed; the code already no-ops gracefully — documented it as a DELIBERATE stub (the flywheel collects distillation pairs; the local-training launcher is a future step that activates unchanged when a trainer is dropped in). (The third §5 item — the `/api/csf/search` → `src/csf_search.py` broken endpoint — was already resolved by its removal in #2534; `tests/perf_dream_journal.py` by #2536.) node --check clean on both files.
