### Added

- trader: **public live track record** (`/track-record.html` + `GET /api/trading/track-record`) — the autopilot books' settlement-graded record published for logged-out visitors: confirmed (broker-accepted) fills only, daily realized P&L with cumulative curve and max drawdown at equal prominence, per-exit-path table with structural win rates flagged, every exclusion disclosed as a count, and the Champion book honestly `pending` until it writes a ledger of its own (#3246, #3247). Registered as a CORE Converge surface; page is public in `routes/pages.js` and the JSON is on `PUBLIC_TRADING_READS` (book-level only — no balances, quantities, or order ids).
- trader: **scorecard breakdown slices** — `GET /api/trading/scorecard?by=symbol|hour|reason|skip` slices the ledger with the same honesty pipeline as the headline scorecard (no-fill attempts dropped and re-decisions collapsed BEFORE bucketing, confirmed-vs-all split per slice, profit-only exit reasons flagged); `by=skip` groups the skip log by number-normalized decline reason (#3240 progress, #3243 v1 API). `lib/trader-scorecard.js` now honors the same `TRADER_TRADES_LOG` override as its writer.
