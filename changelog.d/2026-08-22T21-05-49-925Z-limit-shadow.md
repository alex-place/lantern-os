### Added

- Limit shadow (journal-only, default on; TRADER_LIMIT_SHADOW=0 disables): on every real entry the engine records the resting limits it did not place (0.25/0.5/0.75/1.0% under the touch) and marks which ones the mark touches during the session; experiments/limit_shadow_score.js joins them to the exits — the forward test the 26y bar cannot run for an intraday limit
