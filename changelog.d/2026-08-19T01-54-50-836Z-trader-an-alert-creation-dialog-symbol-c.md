### Added

- trader: an alert creation dialog. Symbol, condition, trigger, message and notifications, laid out like TradingView's — the condition row offers all thirteen price operators plus signal, zone and washout, and reveals a second value box only for the channel operators. Trigger chooses once-only or every time. The drawing toolbar's alert button now creates a real price alert at that line's level instead of just opening the panel (#3352)
