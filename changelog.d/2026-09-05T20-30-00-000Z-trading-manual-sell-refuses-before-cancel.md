### Fixed

- trading: **a closed-market manual sell with no quote now refuses up front — before the cancel-resting-stops step runs.** The naked-book sequence was live on master: the manual-sell path cancels a position's protective stop before placing the sell (2026-08-27, the share-reservation fix), and the no-quote closed-market branch then degraded the order to a raw MARKET+DAY order the broker rejects or expires — so one click could cancel a position's stop and place nothing, leaving it held and unprotected until the engine's next sweep (exactly the race book's 2026-08-28 incident, bd1002ac, now ported). The response says plainly that nothing was placed and no stops were touched. Regression test updated to its third generation (v1 asserted "queues" — false, #3327; v2 asserted the degrade+warning; v3 asserts the refuse). Also files two ledger verdicts: `entry-stagger` REVERSED (deferring same-scan multi-entries cut return without touching the fade-day losses — fourth tool rejected against the pile-in class, which now stands as the engine's drawdown budget rather than a fixable leak) and `csp-shadow-book` INVALIDATED (1,776 rows, zero shadow trades — no options-chain source on the box; the #3220 comparison never started).
