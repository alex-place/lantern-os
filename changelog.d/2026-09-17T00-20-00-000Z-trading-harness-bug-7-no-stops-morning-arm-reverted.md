### Fixed

- trading: **harness bug #7 — every single-harness replay on 09-16 ran without protective stops, and the morning-gate arm it justified is reverted.** The unique-order-id repair declared its tag inside the variant loop, out of scope for the mock broker's closure; every stop placement threw, the engine's fail-soft catch swallowed it, and no stop ever entered the book (the run before the patch shows 167 stop fills, every run after it zero). That voids the day's leave-one-out table, the shape re-validation, the race-as-race sweep and combos, and the morning-gate validation. Re-measured on a harness that places stops: morning 0.12 gives 5.83% at 0.96% drawdown and 61% positive days versus 0.30's 4.92% at 1.38% and 56% — so `TRADER_IBS_MAX_MORNING` returns to 0.12 on stable before the 09-17 open (ledger row `morning-relax-030` → INVALIDATED_BY_FIDELITY; it never fired live). Race's regime dial survives the same re-measurement (7.28% → 8.07%, drawdown 3.26% → 3.13%, first half up, second half identical) and is filed as `race-regime-dial-arm`, OPEN. Every harness now carries a zero-stop-fill tripwire beside the zero-trade one.
