### Fixed

- trader-ui: placed drawings are editable. Click one to select it, drag its handles to resize, drag its body to move it, right-click for colour/width/dash, and press Delete to remove it — style saves on the drawing itself. Also: the cursor category now actually drives the pointer (Cross gives a crosshair instead of the pan hand), the favourites bar defaults over the chart instead of behind the left dock, magnet/keep-drawing/lock are gone, and the ? tooltip reads Learn more (#3354)
