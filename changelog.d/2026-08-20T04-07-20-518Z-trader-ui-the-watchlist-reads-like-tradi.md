### Changed

- trader-ui: the watchlist reads like TradingView's — a sticky Symbol / Last / Chg / Chg% header sitting exactly over its columns, a new absolute day-change column beside the percent, tabular figures so digits line up down the list, and rows at 32px instead of 54 so twelve fit where seven did (#3355)
