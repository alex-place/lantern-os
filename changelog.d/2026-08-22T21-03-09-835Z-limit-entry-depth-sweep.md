### Changed

- Limit-entry depth sweep: limit-filled trades earn +0.32–0.44%/trade vs +0.10% for touch entries (WR 65–67%), and on the hourly surfaces a 0.5–0.75% limit cuts drawdown ~45% and worst months ~70% on BOTH halves; the session-end fallback is the adverse-selection trap (WR 47–55%, negative); daily bars cannot express an intraday limit (next-session fill is a different, worse strategy) so the 26y bar is not a fair judge — forward shadow is the path
