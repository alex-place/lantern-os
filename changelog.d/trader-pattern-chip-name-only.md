change(trader): the pattern chip shows the pattern name, not its category

A detected pattern used to render as "REVERSAL Double Top" — the category read as a
second, louder claim about what the market was about to do, on top of the name. The chip
now shows "Double Top"; the category still rides in the tooltip alongside the engine's
evidence (#3140).
