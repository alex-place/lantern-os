### Removed

- game: migrated the Three Doors / Kingdome of Hearts game out of the monorepo into its own repository, [alex-place/three-doors](https://github.com/alex-place/three-doors), extracted with `git filter-repo` so every migrated file keeps its full commit history (144 commits). Moved: both `skills/three-doors*` skill dirs, the web game (`three-doors-game.html` + its js/css/assets + KOH gallery manifest), the `/api/doors/*` route, the Python engine + classifier, the imagegen script + hand-drawn character refs (closes the #2526 orphan decision — it lives in the game repo now), `data/three-doors*` world logs, the Kingdome SD prompt library (`data/prompts/`), 4 game docs, 6 training scripts, and 11 game test files. Removed from the monorepo in the same pass: the `surface:"three-doors"` RP branch in `lib/stream-chat.js` (the `!three-doors`/`!doors` bangs, `THREE_DOORS_PREAMBLE`, `isRpMode` plumbing, the dead `!choose-future-door` handler, the orphaned `stream-chat/helpers.js`), the game LoRA endpoints in `routes/training.js`, the Discord bot's `/threedoors` commands (snapshot preserved in the new repo), the Explore feed's Kingdome gallery + game embed + chip, the convergence-agent Kingdome cards, the `game` surface-registry module, and the KC doc card (doc-catalog + knowledge index regenerated). The general Dream-Chat `[DOORS:]` chip parser, `lib/image-generation.js` (dream-journal enrichment), and the CSF Status-Cube store stay — the new repo vendors its own `status_cube` snapshot.
