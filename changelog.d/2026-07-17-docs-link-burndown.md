### Fixed

- docs: burned down all 124 dead relative links under docs/** and flipped the link gate to `--strict` (#2600). Breakdown: **92** were root-relative authoring bugs (links written as if from the repo root inside nested docs — e.g. `docs/AGENT.md → CLAUDE.md`) recomputed to correct relative paths; **3** repointed to their unique tracked file; **27** genuinely-rotted targets de-linked to inline code (deleted docs like `OURO-LOOPLM.md`/`TESSERACT-CONVERGENCE-LOOP.md`, untracked `data/research/reports/*`); **2** repointed to surviving equivalents found by hand — `SIGMA0-EV-GATE.md` now cites `lib/signal-engine/convergence-ev.js` (the port of the retired `src/trading_agents/convergence_ev.py`) and `SIGMA0-FRONTIER-TRAIN-BRIEF.md` cites ADR-0024 (renumbered from 0023 in the collision fix). Repo-wide `--strict` sweep: **1,127 relative links, 0 dead**; injected docs/ rot now exits 1. The pr-gates job runs `--strict`, so link rot blocks everywhere from now on.
