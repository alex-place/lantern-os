### Changed

- trader-ui: the drawing toolbar looks and works like TradingView's — a colour button showing the current colour as a swatch, a line-width button with a live preview and a px readout, settings, alerts, lock, delete, and a More menu with duplicate / bring to front / send to back / hide. Locking a drawing keeps it selectable so it can be unlocked, but blocks dragging; hidden drawings stop rendering (#3354)
