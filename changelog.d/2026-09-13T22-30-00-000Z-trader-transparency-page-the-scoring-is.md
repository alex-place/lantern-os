### Added

- trader: **the transparency page — the scoring is the centerpiece** (#3598). One read-only feed (`GET /api/trading/transparency`) assembles the honesty surface from files that already exist: the prediction ledger's per-instrument scoreboard (hit rates count resolved verdicts only; reversals stay on the page — that is the point), open predictions with due dates, recent verdicts with their evidence strings, and the autopilot journal's session strip (equity, day P&L, exits-by-reason — live fills, not backtest curves). `public/transparency.html` renders it with discipline counters up top (predictions filed / scored this week / reversals kept visible / open awaiting tape) — the strategy-factory genre's spectacle counters, pointed at scoring instead of searching. No performance promises anywhere on the page, by design and by copy. Gated like its scorecard/track-record siblings; the public-vs-Pro flip (#3598) stays a one-line decision. Three route tests pin the honesty math (open rows don't dilute hit rates; session rows only in the strip).
